获得要删除节点的父节点的引用，如果要删除的节点是根节点，那么是获得对根节点的引用。因为父节点可以获得子节点引用，而子节点无法获得父节点引用。
对于要删除节点没有儿子的情况，直接将父节点对这个子节点的引用设置为Null。
对于要删除节点只有一个儿子的情况，直接将父节点的引用设置为其子节点。
对于要删除的节点有两个儿子的情况，将右子树的最小数据代替该删除节点（注意删除节点之后再更改数值，因为树是没有重复元素的），递归删除右子树中最小数据节点。